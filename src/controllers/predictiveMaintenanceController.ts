// import predictiveMaintenanceService from "../services/predictiveMaintenanceService";

// // ในฟังก์ชันของ controller
// async function checkMachineStatus(req: Request, res: Response) {
//   const { machineId } = req.params;
//   try {
//     const result = await predictiveMaintenanceService.predictMaintenance(
//       machineId
//     );
//     res.json(result);
//   } catch (error) {
//     res.status(500).json({ error: "Failed to predict maintenance needs" });
//   }
// }
